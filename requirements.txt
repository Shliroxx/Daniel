# --- Kern ---
anthropic>=0.69.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.1
python-multipart>=0.0.12   # Datei-Upload fuer /api/stt (Aufnahme vom Handy)
pydantic>=2.9.0
httpx>=0.27.0

# --- Sprache (alles lokal / offline) ---
faster-whisper>=1.1.0      # Speech-to-Text
openwakeword>=0.6.0        # Wake-Word "Hey Jarvis"
piper-tts>=1.2.0           # Text-to-Speech
sounddevice>=0.5.0         # Mikrofon + Lautsprecher
soundfile>=0.12.1
numpy>=1.26.0
scipy>=1.13.0

# --- PC-Steuerung / Systeminfos ---
psutil>=6.0.0
mss>=9.0.1                 # Screenshots
Pillow>=10.4.0

# --- Nur Windows (optional, wird automatisch übersprungen) ---
pycaw>=20240210; sys_platform == "win32"
comtypes>=1.4.0; sys_platform == "win32"
